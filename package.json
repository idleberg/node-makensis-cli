{
  "name": "makensis-cli",
  "version": "0.11.1",
  "description": "CLI for the makensis wrapper on Node, compiles NSIS scripts",
  "main": "index.js",
  "bin": {
    "mn": "./index.js",
    "nsis": "./index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/idleberg/node-makensis-cli.git"
  },
  "keywords": [
    "nsis",
    "makensis",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "@nsis/language-data": "^0.6.0",
    "commander": "^2.19.0",
    "generator-nsis": "^0.8.0",
    "makensis": "^0.17.0",
    "update-notifier": "^2.5.0",
    "yeoman-environment": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^11.10.4",
    "tslint": "^5.13.1",
    "typescript": "^3.3.3333"
  },
  "scripts": {
    "build": "tsc --pretty --project ./",
    "dev": "tsc --watch --pretty --project ./",
    "prepublishOnly": "tslint --project . && npm run build",
    "test": "tslint --project ."
  }
}
